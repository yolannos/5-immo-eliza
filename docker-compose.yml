version: "3"

services:
    yolann_api:
        build:
            context: ./
            dockerfile: Dockerfile
        image: immo_elisa_api
        ports:
            - 8501:8501
        volumes:
            - ./src/